services:
  ft_traceroute:
    build: .
    container_name: ft_traceroute
    volumes:
      - .:/ft_traceroute
    # allow raw sockets
    cap_add:
      - NET_RAW
      - NET_ADMIN
    stdin_open: true
    tty: true
    network_mode: bridge
    command: /bin/bash
